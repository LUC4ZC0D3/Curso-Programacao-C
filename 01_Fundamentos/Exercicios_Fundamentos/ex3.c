#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

int main()
{
    int a, b, c;

    scanf("%d %d %d", &a, &b, &c);

    // resultado = a * b * c;

    printf("Resultado: %d", a * b * c);
}